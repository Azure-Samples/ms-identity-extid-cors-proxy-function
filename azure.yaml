# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ms-identity-ciam-cors-proxy
metadata:
  template: functions-quickstart-javascript-azd@0.0.2-beta
services:
  proxy:
    project: .
    language: js
    host: function
    remoteBuild: true
